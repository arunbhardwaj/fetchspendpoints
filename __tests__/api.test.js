const { balances } = require("../src/models")

test('it should store user transactions', () => {
  // check transactions

})

test('it should update user balance when transaction is entered', () => {

})

test('it should update user balance when transactions are entered', () => {

})

describe('PUT /balance', () => {
  it('should always have a timestamp on request body', () => {

  })

  // should return json
  // should return status code

})

describe('GET /balance', () => {
  // should return json
  // should return status code
})